#include "main.h"

/**
  * _strcmp - compares two strings
  * @s1: first string
  * @s2: second string
  * Return: equal rt (0),s1 > s2 rt (>0), s1 < s2 rt (0<)
  */
int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
