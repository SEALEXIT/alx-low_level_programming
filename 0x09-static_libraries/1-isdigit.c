#include "main.h"
/**
  * _isdigit - Is character numeric Character ?
  * @c: the ASCII value of character c
  * Return: Non Zero if character is numeric Else Zero if not numeric
  */
int _isdigit(int c)
{
	return (isdigit(c));
}
