#include "main.h"
/**
  * _abs - Removes the sign before the integer making the integer a positive one
  * @n: The integer with a sign
  * Return: Positive integer
  */
int _abs(int n)
{
	return (abs(n));
}

