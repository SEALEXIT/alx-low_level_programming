#include "main.h"
/**
  * _strcpy - coppies string
  * @dest: where the string is to be coppied to
  * @src: Source of the string to be coppied
  * Return: the new string
  */
char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}

