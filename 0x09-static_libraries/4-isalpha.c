#include "main.h"
/**
  * _isalpha - Checks if character is an alphabet a-z/ A- Z or not
  * @c: Character passed
  * Return: If character  passed is alphabet , return Non Zero (0 >) if not alphabet return Zero (0)
  */
int _isalpha(int c)
{
	return (isalpha(c));
}

