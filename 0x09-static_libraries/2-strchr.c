#include "main.h"

/**
  * _strchr - Looks for character in a string
  * @s: The string
  * @c: the character
  * Return: Address of the character in the string
  */

char *_strchr(char *s, char c)
{
	return (strchr(s, c));
}

