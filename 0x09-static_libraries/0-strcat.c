#include "main.h"

/**
  * _strcat - concatinates string to another string
  * @dest: The string to be to concatinated
  * @src: The source of the string to be concatinated
  * Return: The new string with the concatination
  */

char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
