#include "main.h"

/**
  * _islower - checks if character is lowercase
  * @c: the character
  * Return: Non Zero character is lowercase  zero if not in lower case
  */
int _islower(int c)
{
	return (islower(c));
}
