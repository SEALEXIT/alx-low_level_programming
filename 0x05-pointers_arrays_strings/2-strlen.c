#include "main.h"
#include <string.h>

/**
  * _strlen - Types the length of a string
  * @s: The memory adress of the string
  * Return: Length of the string
  */

int _strlen(char *s)
{
	int length;
	length = strlen(s);

	return (length);
}
