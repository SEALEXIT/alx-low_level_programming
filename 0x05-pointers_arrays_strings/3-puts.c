#include "main.h"
#include <stdio.h>

/**
  * _puts - Prints string followed by new line
  * @str: the memory location of the string
  * Return: No return Value
  */

void _puts(char *str)
{
	puts(str);
}
